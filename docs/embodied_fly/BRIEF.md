# Approved embodied-fly brief

Preserve the emergent multi-agent survival lab, its mechanical swatter, heat lamp,
finite food/water, refuges and selected-fly inspector. Course-correct the neural
architecture so learning includes body locomotion and utility inside one actor.

The user explicitly reconfirmed that Utility AI stays: learn activity scores with
the MaleCNS-based core, then let the same core produce individual limb/wing commands.
Do not present a tiny learned selector over an existing gait as a learned fly brain.

Use measured MaleCNS topology, train internal neural computation and interfaces,
retain per-fly recurrent memory, and extend one checkpoint through a staged
curriculum. Prove stand/walk/stop/turn first; then recovery, wings/flight/transitions,
needs, hazards and interacting flies. Show actual neural/utility state in the UI.
No teacher or scripted gait at student evaluation; no base teleport or hidden support.

Use GPU when useful, measure short pilots, preserve failures and report inherited
teacher knowledge separately from our training. Full locomotion and survival must
be demonstrated physically before claiming an integrated trained fly controller.
The accepted detailed plan is [the course correction](../fly_survival/COURSE_CORRECTION.md).
