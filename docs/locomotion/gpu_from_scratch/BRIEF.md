# Measure GPU walking from random weights

Work started September 12, 2026 at **05:14:54 UTC**. User request: “then lets run it for real and really time it, stop guessing and lets do what really needs to be done”. The previous 42-second summary summed three pretrained continuation benchmarks; it did not measure acquisition of walking and must not be used as that result.

**Current scope, confirmed by the user:** **4,096 GPU worlds throughout**, one evolving actor, the successful recorded reward curriculum, and one final checkpoint for walking, standing and moving-support commands. `CURRICULUM.json` reconstructs the 21 walking stages and the seven standing/two moving extensions. Preserve recorded learning rates, seeds, reward sequence, 24-step rollouts, four epochs and 3,072-sample minibatches. Historical 512-world walking sample counts are rounded up to complete GPU rollouts: **39,419,904 walking transitions**, 2.66% above 38,400,000; standing/moving retain **32,342,016**, for **71,761,920 planned transitions**. Do not present this as an exact-sample CPU speed comparison. No reward retuning. All trained references are rebuilt from this run's own checkpoints. Each stage has a five-minute protective cap and must complete its declared updates; a capped/failed stage cannot silently become a successful result.

Historical final minibatches were sometimes time-limited. This replay completes all four epochs and uses modern compatible code; bitwise reproduction is not claimed. After stage 30, evaluate all commands with that one final checkpoint, including walking and static retention. The earlier film's three historical checkpoints are not a substitute for this final shared-weight verification. Moving-support training still covers the healthy robot; damaged moving transfer remains a separately labeled untrained test.

The user subsequently requested only necessary checks. Use one development walking-retention evaluation after walking (stage 21) and after standing (stage 28): four trials per body, seed 9501, existing healthy gait gates. Then run one combined final audit of stage 30, four trials per condition with holdout seed 9507: all nine walking bodies, healthy static surfaces, damaged flat holds, walk–hold–walk transitions and healthy moving supports. This replaces the larger evaluation counts in the original plan below. Evaluate copied frozen checkpoints on the Mac while GPU training continues; those CPU evaluations are not GPU learning time. No automatic reward changes or extra training follow a failed check. Early standing stages use walking-reference retention; later standing and moving stages also use saved walking rehearsal, collected from this run's own walker.

The three earlier 4,096-world rounds below were an exploratory detour, not the requested recipe replay. Their outputs and measured time remain separate under `healthy_r1` through `healthy_r3`. A subsequent attempt to match the old CPU world's count was also incorrect for the user's requested GPU configuration: `replay_512` was stopped at **05:37:38 UTC** during stage 5, and its recipe is preserved in `CURRICULUM_512_ARCHIVED.json`. Neither detour is continued. The actual replay starts with fresh seed-2 random weights under `replay_4096` and reuses neither exploratory nor historical trained weights. The following initial plan is retained as history, superseded where it conflicts with this paragraph.

Train on the RTX 4090 with MuJoCo Warp physics and CUDA actor/critic learning. Begin with a randomly initialized healthy walker, then extend its own weights to the nine accepted physical body configurations. Do not initialize from historical trained weights or use their motion datasets as training targets. Existing checkpoints may be used for evaluation comparisons only. If healthy-motion guidance is needed, collect it from the healthy policy trained in this run and account for its ancestry and collection overhead.

Use the existing network, body models, torque limits, contacts, 2 ms physics and 20 ms control. Start with 4,096 worlds, PPO horizon 24, four epochs and minibatches of 3,072. First healthy round: 120-second learning budget, random seed 31, learning rate 0.001; established walk/support/stride/balance/body-motion rewards with weights 2/1/5/0.5 respectively. No teacher, action symmetry constraint or gait script. Evaluate before extending training. Keep individual rounds within five minutes; decide further allowance from measured progress, as previously authorized.

Development seed 9501: initially eight complete twelve-second trials per trained body at 0.5 ms evaluation physics, with allowed support measured every substep. Basic walking must reach 5 m, hold the goal for one second, and survive the complete trial using only designated supports. Record torque, penetration, individual foot swing/stance, stride, slip and body motion. A basic task pass is separate from matching the accepted gait: retain the existing healthy 28–35 cm stride, 12 percentage-point duty gap and 11 mm height-variation gates, and all-foot visible-step checks when comparing the adaptive policy. Do not relax failures to make a short time claim.

Freeze a candidate before holdout seed 9507 (64 healthy trials or eight trials per body for the adaptive stage), then record predetermined video trial 0 with seed 9511. If short training does not match the accepted gait, report that separately from learning to move. Preserve failed rounds, raw logs and previous demos.

Record setup/import/compilation, learning-loop time, evaluation, rendering and total elapsed work separately. Use logged transition and optimizer counts, source commits, initial/final checkpoint hashes and actual measured clocks. Do not extrapolate CPU times or sum independent repeats as one policy's learning time. Store source/results/media on this branch and synchronize with commits and LFS.

Timing clarification: walking evaluation is outside the GPU learning timer. Walking-reference and rehearsal losses inside PPO are actual learning work and remain included. Offline rehearsal-data collection is setup, reported separately. Saved examples reused during optimization are not additional worlds or new simulated transitions. Training-loop timing ends after device synchronization and includes ordinary in-loop checkpoint writes; it is not isolated neural-network kernel time.
